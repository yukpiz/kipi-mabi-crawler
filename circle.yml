machine:
  python:
    version: 2.7
  node:
    version: 6.3.0
  timezone: Asia/Tokyo

test:
  pre:
    - echo "(」・ω・)」"
    - echo "(/・ω・)/"

dependencies:
  pre:
    - pip install lamvery
    - npm install -g npm@4
    - npm install

deployment:
  master-head:
    branch: master
    commands:
      - pwd
      - cd ./request/
      - pwd
      - cd `pwd`/request/
      - lamvery deploy
