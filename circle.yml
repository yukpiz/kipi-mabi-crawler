machine:
  python:
    version: 2.7
  node:
    version: 6.3.0
  timezone: Asia/Tokyo

dependencies:
  pre:
    - |
      pip install lamvery
      npm install -g npm@4
      npm install

deployment:
  master-head:
    branch: master
    commands:
      - |
        lamvery deploy

