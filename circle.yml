machine:
  node:
    version: 6.3.0
    post:
      - npm install -g npm@4
    timezone: Asia/Tokyo

dependencies:
  pre:
    - |
      npm install

deployment:
  master-head:
    branch: master
    commands:
      - |
        lamvery deploy

